name: sent_env
channels:
  - defaults
dependencies:
  - python=3.9
  - pytorch
  - scikit-learn
  - pandas
  - numpy
  - ipykernel
  - pip
  - jupyterlab
  - jupyter
  - tensorboard
  - matplotlib
  - pip:
    - gdown
prefix: /home/anaconda3/envs/sent_env
